import { NotificationsFilterPipe } from './notifications-filter.pipe';

describe('NotificationsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new NotificationsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
